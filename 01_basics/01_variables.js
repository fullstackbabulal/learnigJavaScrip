const accountId = 144553; // Account ID is constant, cannot be changed

let accountEmail = "hitesh@google.com"; // Email can be changed

var accountPassword = "12345"; // Password can also be changed

accountCity = "Jaipur"; // City is declared without any keyword

let accountState; // State variable is declared but not assigned a value

// Changing the values of variables
accountEmail = "hc@hc.com";
accountPassword = "21212121";
accountCity = "Bengaluru";

// Console logging accountId
console.log(accountId);

// Printing variables in a table format
console.table([accountId, accountEmail, accountPassword, accountCity, accountState]);
